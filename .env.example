# Deep Learning Computer Vision Pipeline — environment overrides (all optional)
# Device for inference: cpu (default) or cuda
DEVICE=cpu
# Minimum confidence score for reported detections (0.0 - 1.0)
SCORE_THRESHOLD=0.5
# Log level
LOG_LEVEL=INFO
